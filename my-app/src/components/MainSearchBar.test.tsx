/*import React from 'react';
import { shallow } from 'enzyme';
import Src/components/MainSearchBar from './Src/components/MainSearchBar';

describe('<Src/components/MainSearchBar />', () => {
  let component;

  beforeEach(() => {
    component = shallow(<Src/components/MainSearchBar />);
  });

  test('It should mount', () => {
    expect(component.length).toBe(1);
  });
});
*/